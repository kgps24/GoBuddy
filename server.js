require('dotenv').config();
const express=require('express');const cors=require('cors');const crypto=require('crypto');const Razorpay=require('razorpay');const path=require('path');
const app=express();app.use(cors({origin:process.env.WEB_ORIGIN||true}));app.use(express.json());app.use(express.static(__dirname));
const razorpay=process.env.RAZORPAY_KEY_ID&&process.env.RAZORPAY_KEY_SECRET?new Razorpay({key_id:process.env.RAZORPAY_KEY_ID,key_secret:process.env.RAZORPAY_KEY_SECRET}):null;
app.get('/',(req,res)=>res.sendFile(path.join(__dirname,'index.html')));app.get('/api/health',(req,res)=>res.json({ok:true,service:'gobuddy-api'}));
app.post('/api/payments/order',async(req,res)=>{try{if(!razorpay)return res.status(503).json({error:'Payment provider not configured'});const amount=Number(req.body.amount);if(!Number.isInteger(amount)||amount<100)return res.status(400).json({error:'Invalid amount'});const order=await razorpay.orders.create({amount,currency:'INR',receipt:`ride_${Date.now()}`,notes:{rideId:String(req.body.rideId||'')}});res.json({id:order.id,amount:order.amount,currency:order.currency})}catch(e){res.status(500).json({error:'Could not create order'})}});
app.post('/api/payments/verify',(req,res)=>{const{razorpay_order_id,razorpay_payment_id,razorpay_signature}=req.body;if(!process.env.RAZORPAY_KEY_SECRET)return res.status(503).json({error:'Payment provider not configured'});const body=`${razorpay_order_id}|${razorpay_payment_id}`,expected=crypto.createHmac('sha256',process.env.RAZORPAY_KEY_SECRET).update(body).digest('hex'),provided=String(razorpay_signature||'');if(expected.length!==provided.length)return res.status(400).json({verified:false});const ok=crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(provided));res.status(ok?200:400).json({verified:ok})});
if(require.main===module){const port=process.env.PORT||3000;app.listen(port,()=>console.log(`GoBuddy running at http://localhost:${port}`))}module.exports=app;
